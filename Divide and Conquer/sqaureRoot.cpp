#include<bits/stdc++.h>
using namespace std;
double squareRoot(int n,int p){
    int l=0,h=n,mid;
    double ans;
    while(l<=h){
        mid=(l+h)/2;
        if(mid*mid==n){

        }
    }
}
int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int n,p;
    while(cin>>n>>p){
        cout<<squareRoot(n,p);
    }
    return 0;
}
